#include <intrin.h>
#include "eventlogs.h"

extern event_logs eventlog;

void event_logs::OnUpdate() {
 //   Draw();

	/*
	for (int i = 0; i < 50; i++) {
		for (int b = 0; b < 10; b++) {
			if (setuped[i][b]) {
				if (x[i][b] > 300) {
					reset[i][b] = true;
				}
				else if (!reset[i][b]) {
					x[i][b] += 10;
				}

				if (reset[i]) {

					timer[i][b]++;

					if (timer[i][b] > 150) {
						x[i][b] -= 10;
						if (x[i][b] <= -300) {
							timer[i][b] = 0;
							setuped[i][b] = false;
						}
					}
				}
			}
			form[i][b] = { -300 + x[i][b], 20 + y[i][b], 0 + x[i][b], 80 + y[i][b] };
		}
	}
	
	*/
	
		
	
}

